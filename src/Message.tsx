import { MouseEvent, useState } from "react";

function Message(){
    let items = ["abc","cda","los"];

    const [selectedIndex,SetSelectedIndex] = useState(-1);

    return <>
     <ul>
        
     </ul>
    </>;
}

export default Message;